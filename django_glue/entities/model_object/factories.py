from django_glue.entities.model_object.entities import GlueModelObject
from django_glue.handler.data import GlueBodyData
from django_glue.session import GlueSession


def glue_model_object_from_glue_session(glue_session: GlueSession, body_data: GlueBodyData) -> GlueModelObject:
    # Todo: Return glue model object
    return GlueModelObject()
